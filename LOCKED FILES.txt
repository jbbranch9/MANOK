Files which should not be changed at all:

zones.gd

Files which should be considered add-only (no deletions or non-debugging alterations):

globals.gd

Files scheduled for deprecation and/or deletion (no new dependencies):

cursor.tscn
cursor.gd
carrier_stage.gd
carrier_stage.tscn